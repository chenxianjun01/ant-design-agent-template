# Chatbot Agent Day Task 2026-04-20

## 1. OpenSpec 归档状态

今天检查了 `openspec/changes/` 当前状态。

结论：

1. 当前没有可继续归档的 active change。
2. `openspec/changes/` 下只保留了 `archive/` 目录。
3. 之前的 chatbot 相关变更已经完成归档，包括：
   - `chatbot-agent-runtime-stream-integration`
   - `chatbot-agent-execution-controls`
   - `chatbot-conversation-persistence-recovery`
   - `chatbot-runtime-protocol-hardening`
   - `chatbot-runtime-observability-debugging`
   - 以及更早一批 rich message / schema slot / submit action / mock validation 相关 change

因此今天没有执行新的 OpenSpec archive 动作，原因不是遗漏，而是仓库当前已经没有待归档的 active 变更。

---

## 2. 今日工作主题

今天的主要工作不是继续扩功能，而是对 `src/pages/chatbot` 做一次系统性的类型收口与页面层拆分，目标是：

1. 减少重复类型定义。
2. 清理 mock / message / action / runtime 的多处平行知识。
3. 让 `index.tsx` 从“大而全页面文件”收敛为“状态组装 + UI 渲染入口”。
4. 为后续继续扩展执行态、消息卡片和 mock 工具条降低维护成本。

---

## 3. 类型系统与模型层收口

### 3.1 统一回调返回类型

修正了 `void | Promise<T>` 这类容易触发 lint 的联合返回类型，并统一为可复用类型：

1. 新增 [src/pages/chatbot/types.ts](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/types.ts:1)
2. 收口：
   - `MaybePromise<T>`
   - `FormSubmitHandler`
   - `AgentExecutionControlHandler`

影响文件包括：

1. [buildBubbleItems.tsx](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/buildBubbleItems.tsx:1)
2. [MessageRenderer.tsx](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/components/MessageRenderer.tsx:1)
3. [TextMessage.tsx](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/components/messages/TextMessage.tsx:1)
4. [SchemaSlot.tsx](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/components/schema/SchemaSlot.tsx:1)

### 3.2 消息组件公共类型拆分

为消息组件层新增了独立类型入口：

1. [src/pages/chatbot/components/messages/types.ts](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/components/messages/types.ts:1)

收口内容：

1. `MessageComponentProps`
2. `MessageActionExecutor`
3. `MessageActionAlertProps`

这样其他消息组件不再反向依赖 `TextMessage.tsx` 暴露公共类型。

### 3.3 data.ts 里的重复字面量联合收口

把 `data.ts` 里多处重复的联合类型整理成共享别名：

1. `MessageTableValueType`
2. `MessageActionButtonType`
3. `TimelineItemStatus`
4. `ApprovalStatus`
5. `AgentExecutionStatus`
6. `AgentExecutionStepStatus`
7. `AgentExecutionControlAction`

同时移除了中间转发别名：

1. `FormSubmitActionMockType`
2. `FormSubmitActionMockChartType`

现在直接复用 `service.ts` 中的：

1. `MockMessageType`
2. `MockChartType`

---

## 4. mockPayloadFactory 收口

今天继续清理了 [src/pages/chatbot/mockPayloadFactory.ts](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/mockPayloadFactory.ts:1)。

### 4.1 mock 内容改为受模型约束

为以下 mock content 补上了 `satisfies` 约束：

1. `IFileMessageContent`
2. `IImageMessageContent`
3. `IAudioMessageContent`
4. `ITableMessageContent`
5. `IFormMessageContent`
6. `IMapMessageContent`
7. `ITimelineMessageContent`
8. `IApprovalMessageContent`
9. `IAgentExecutionMessageContent`

效果：

1. 状态字面量、消息结构、submitAction 字段都开始直接受模型层类型约束。
2. mock 数据和真实数据模型发生漂移时，编译阶段就能尽早暴露。

### 4.2 消息类型字面量改为枚举

把 mock 里的消息 `type` 从裸字符串逐步改成 `MessageType.*`：

1. `text`
2. `table`
3. `form`
4. 以及各种 `insertMessage` / `onSuccessMessage` / `onErrorMessage` 内部消息

### 4.3 图表类型来源去重

删除了本地 `MockChartTypeLike`，直接改为复用 `service.ts` 提供的 `MockChartType`。

---

## 5. service.ts / mock 配置中心化

今天继续把 mock 类型相关知识往 `service.ts` 收。

新增：

1. `isMockMessageType`
2. `isMockChartType`
3. `MOCK_MESSAGE_TYPE_LABELS`
4. `MOCK_CHART_TYPE_LABELS`

效果：

1. mock 类型集合、类型守卫、展示标签统一在一个来源维护。
2. 页面层不再维护同一套信息的本地副本。

---

## 6. 聊天页配置拆分

### 6.1 预设会话迁出页面

新增：

1. [src/pages/chatbot/conversationPresets.ts](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/conversationPresets.ts:1)

迁出内容：

1. `PRESET_CONVERSATIONS`
2. `DEFAULT_CONVERSATIONS`
3. `DEFAULT_ACTIVE_KEY`

这样 `index.tsx` 不再承担预设聊天配置的维护职责。

---

## 7. 聊天页副作用与命令逻辑拆分

今天最大的重构工作，是把 [src/pages/chatbot/index.tsx](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/index.tsx:1) 中混在一起的副作用、命令逻辑和会话控制逐步拆分出去。

### 7.1 运行时 execution 同步逻辑抽 hook

新增：

1. [useRuntimeExecutionSync.ts](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/useRuntimeExecutionSync.ts:1)

抽走内容：

1. runtime event store
2. `recordRuntimeEvent`
3. 远端 `agent-execution` 消息同步到本地 structured message
4. runtime patch stream 轮询
5. stream 停止与清理

### 7.2 聊天消息持久化与 workspace 落盘抽 hook

新增：

1. [useChatMessagePersistence.ts](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/useChatMessagePersistence.ts:1)
2. [useWorkspaceSnapshotPersistence.ts](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/useWorkspaceSnapshotPersistence.ts:1)

分别负责：

1. `parsedMessages -> chatMessageMap`
2. workspace snapshot 保存

### 7.3 starter prompt 自动发送抽 hook

新增：

1. [useStarterPromptBootstrap.ts](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/useStarterPromptBootstrap.ts:1)

负责：

1. 检测当前会话是否需要自动发送 starter prompt
2. 处理 bootstrapped 标记
3. 复用 mock type / chart type 守卫

### 7.4 聊天动作 handler 抽 hook

新增：

1. [useChatActions.ts](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/useChatActions.ts:1)

收口动作：

1. `sendMessage`
2. `handleSenderSubmit`
3. `handleFormSubmit`
4. `handleAgentExecutionControl`

### 7.5 会话控制抽 hook

新增：

1. [useConversationController.ts](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/useConversationController.ts:1)

负责：

1. `newChat`
2. 删除会话后的 `chatMessageMap` 清理
3. structured messages 清理
4. activeKey 切换
5. `Conversations.menu` 生成

### 7.6 mock toolbar 计算抽 hook

新增：

1. [useMockToolbar.ts](/Users/chenxianjun/Desktop/items/agent/schema-flow/src/pages/chatbot/useMockToolbar.ts:1)

负责：

1. `showMockToolbar`
2. `currentMockTypeLabel`
3. message type options
4. chart type options

---

## 8. 当前页面分层结果

经过今天这轮调整，`index.tsx` 的职责已经明显收敛：

### 页面保留的职责

1. 初始化页面级 state
2. 组合 hooks
3. 准备少量展示层 memo
4. 渲染 Conversations / Bubble.List / Sender / DebugPanel

### 已迁出的职责

1. mock toolbar 配置计算
2. 聊天动作执行
3. starter prompt 启动逻辑
4. workspace snapshot 持久化
5. runtime stream 同步与 patch 轮询
6. 会话删除 / 新建 / 菜单控制

这意味着后续继续扩展聊天页时，不需要再在 `index.tsx` 中继续堆叠副作用和动作逻辑。

---

## 9. 验证结果

今天所有重构步骤都持续做了本地校验，最终结果：

1. `npx @biomejs/biome lint ...` 通过
2. `npm run tsc` 通过

本次工作以结构收口、类型统一、页面分层为主，没有引入新的产品能力，但显著降低了聊天模块后续继续演进的维护成本。
