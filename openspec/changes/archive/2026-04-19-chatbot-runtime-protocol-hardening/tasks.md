## 1. OpenSpec And Todo Scope

- [x] 1.1 Add proposal, design, and spec artifacts for runtime protocol hardening
- [x] 1.2 Update `todo.md` with the runtime protocol hardening change and current status

## 2. Contract Hardening

- [x] 2.1 Add a formal runtime protocol contract document covering patch, stream batch, and control result
- [x] 2.2 Align current frontend runtime types and endpoint references with the contract terminology

## 3. Validation And Records

- [x] 3.1 Add or update lightweight validation around the runtime contract references
- [x] 3.2 Update `todo.md`, development record, and OpenSpec usage record
- [x] 3.3 Validate with `pnpm exec jest src/pages/chatbot --runInBand` and `pnpm exec tsc --noEmit`
