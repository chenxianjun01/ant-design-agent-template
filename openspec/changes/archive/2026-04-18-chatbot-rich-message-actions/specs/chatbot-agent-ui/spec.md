## ADDED Requirements

### Requirement: Actionable rich media coverage
The chatbot UI MUST allow rich media messages to trigger follow-up actions without leaving the conversation.

#### Scenario: File, image, audio, and map messages trigger follow-up actions
- **WHEN** users interact with message-level action controls
- **THEN** the chatbot UI inserts follow-up results or sends requests through the shared action engine
