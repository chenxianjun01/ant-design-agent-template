## 1. OpenSpec And Runtime Scope

- [x] 1.1 Add proposal, design, and spec artifacts for SchemaSlot runtime validation
- [x] 1.2 Define runtime validation scope around fallback and submit bridge behavior

## 2. Runtime Test Implementation

- [x] 2.1 Add SchemaSlot tests for renderer override and safe fallback behavior
- [x] 2.2 Add SchemaSlot tests for non-form handling and Formily submit bridge

## 3. Records And Validation

- [x] 3.1 Update the development record with SchemaSlot runtime validation progress
- [x] 3.2 Add an OpenSpec usage record for this change
- [x] 3.3 Validate with `pnpm exec jest src/pages/chatbot --runInBand` and `pnpm exec tsc --noEmit`
